extends layout.pug

block content
  container.header
    h1 Shopping Cart

  container
    table
      thead
        tr
          th(style='width:50%') Product
          th(style='width:10%') Price
          th(style='width:8%') Quantity
          th.text-center(style='width:22%') Subtotal

        tbody
        each item in cartList
          tr

            td(data-th='Product')
              .row
                .col-sm-2.hidden-xs
                  img.img-responsive(src='http://placehold.it/100x100' alt='...')
                .col-sm-10
                  h4.nomargin
                  | #{item.title}
                  p
                    | Quis aute iure reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Lorem ipsum dolor sit amet.
                  button.btn.btn-info.btn-sm Save For Later
                  i.fa.fa-refresh
                  button.btn.btn-danger.btn-sm Remove
                  i.fa.fa-trash-o 
            td(data-th='Price') 
                    | #{item.price}
            td(data-th='Quantity')
              input.form-control.text-center(type='number' value='1')
            td.text-center(data-th='Subtotal') #{item.price}        
        tfoot
          tr
            td
              a.btn.btn-warning(href='#')
                i.fa.fa-angle-left
                |  Continue Shopping
            td.hidden-xs(colspan='2')
            td.hidden-xs.text-center
              strong Total #{cartTotal}
            td
              a.btn.btn-success.btn-block(href='#')
                | Checkout 
                i.fa.fa-angle-right

  container.header
    h1 Saved for Later

  container
    table
      thead
        tr
          th(style='width:50%') Product
          th(style='width:10%') Price
          th(style='width:8%') Quantity
          th.text-center(style='width:22%') Subtotal

        tbody
        for item in saveLaterList
          tr

            td(data-th='Product')
              .row
                .col-sm-2.hidden-xs
                  img.img-responsive(src='http://placehold.it/100x100' alt='...')
                .col-sm-10
                  h4.nomargin
                  | #{item.title}
                  p
                    | Quis aute iure reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Lorem ipsum dolor sit amet.
                  a.button.btn.btn-info.btn-sm(onclick='addToCart('+item.bookId +')') Add
                  i.fa.fa-refresh
                  button.btn.btn-danger.btn-sm Remove
                  i.fa.fa-trash-o
                  td(data-th='Price') 
                    | #{item.price}
            td(data-th='Quantity')
              input.form-control.text-center(type='number' value='1')
            td.text-center(data-th='Subtotal') #{item.price}
